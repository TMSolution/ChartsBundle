{% if options.chartType=='easy.pie.mini' %}
    {
        execute: function(){
          {{ include('TMSolutionChartsBundle:Templates:easy.pie.mini.template.js.twig') -}}
        }
    }
{% endif %}